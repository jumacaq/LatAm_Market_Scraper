# FILE: Proyecto/job-market-intelligence/requirements.txt
# Scraping
scrapy>=2.11.0
beautifulsoup4>=4.12.2
selenium>=4.15.2
requests>=2.31.0
webdriver-manager>=4.0.1 # Para Selenium, aunque no se usa en los spiders actuales. Mantenido por si acaso.
lxml>=4.9.3 # Parser para BeautifulSoup, más rápido.
fake-useragent>=1.4.0 # Para middlewares.py

# Database
supabase>=2.3.0
python-dotenv>=1.0.0

# Data Processing & Analysis
pandas>=2.1.4
numpy>=1.26.2
plotly>=5.18.0
altair>=5.2.0 # Agregado para gráficos más elegantes en Streamlit
scikit-learn>=1.3.2 # Mantenido si hay planes de ML futuro
pyyaml>=6.0.1 # Para cargar config.yaml
python-dateutil>=2.8.2 # Útil para manejo de fechas
# tabulate>=0.9.0 # No es estrictamente necesario, pero útil para CLI si se usa

# Dashboard
streamlit>=1.29.0
# Se recomienda usar las versiones más recientes y compatibles de Streamlit y Altair.

# AI/Generative Models
google-generativeai>=0.3.0

# Scheduling (si se usa scheduler.py)
schedule>=1.2.1

# Excel (si se usa para reportes específicos)
openpyxl>=3.1.2
xlsxwriter>=1.4.0

